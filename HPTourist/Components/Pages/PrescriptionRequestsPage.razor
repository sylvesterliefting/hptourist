@page "/prescription-requests"
@inject IStringLocalizer<PrescriptionRequestsPage> Localizer

<h3>@Localizer["PrescriptionRequests"]</h3>
<PrescriptionRequests PageNumber="@PageNumber" PageParameterName="page" />

@code {
    [SupplyParameterFromQuery(Name = "page")]
    public int PageNumber { get; set; }
}